{
   "_comment":"Please note that these parameters are fundamental parameters determined by the benchmark;changing them arbitrarily will result in errors!",
   "modality":"text",
   "voting":"weighted",
   "seed":42,
   "description_path":"./configs/description/Routerbench.json",
   "device": "cuda:0",
   "dataset": {
    "name": "Routerbench",
    "dataset_dir": "./data/routerbench",
    "split": {
      "mode": "in-domain",
      "ratios": {
        "train": 0.2,
        "test": 0.8
      }
    }
  },
  "embeddings": {
    "text_model": "all-MiniLM-L6-v2",
    "image_model": null,
    "batch_size": 32,
    "normalize": true,
    "training": false,
    "out_dim": 384
  }
}